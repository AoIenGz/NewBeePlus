# MyGo 能效标签与缺陷检测系统 — 系统设计文档

## 文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | MyGo 能效标签与缺陷检测系统 |
| 文档版本 | V2.0 |
| 编写日期 | 2026-04-19 |
| 文档状态 | 已完成 |

---

## 一、系统架构设计

### 1.1 系统总体架构

本系统采用三层架构设计，分为前端应用层、后端服务层和 AI 引擎层。

```mermaid
graph TB
    subgraph 前端应用层["前端应用层 (OpenHarmony + ArkTS)"]
        A1[Index.ets<br/>主页面-双Tab模式]
        A2[CameraPage.ets<br/>相机拍照]
        A3[History.ets<br/>历史记录+统计]
        A4[Settings.ets<br/>系统设置]
        A5[Interfaces.ets<br/>类型定义]
    end

    subgraph 后端服务层["后端服务层 (Node.js + Express + sql.js)"]
        B1[server.js<br/>Express入口 端口3000]
        B2[detectionRoutes.js<br/>检测路由]
        B3[historyRoutes.js<br/>历史路由]
        B4[productRoutes.js<br/>产品路由]
        B5[detectionService.js<br/>检测服务]
        B6[historyService.js<br/>历史服务]
        B7[database.js<br/>SQLite数据库]
    end

    subgraph AI引擎层["AI引擎层 (Python + YOLOv8 + PaddleOCR + OpenCV)"]
        C1[detect_server.py<br/>常驻HTTP服务 端口5001]
        C2[detect_api.py<br/>单次检测脚本-spawn备用]
        C3[webcam_server.py<br/>网络摄像头服务 端口5000]
        C4[best.pt<br/>YOLOv8n模型 6.25MB]
    end

    A1 -->|HTTP/JSON| B2
    A3 -->|HTTP/JSON| B3
    A4 -->|HTTP/JSON| B4
    B2 --> B5
    B3 --> B6
    B5 -->|HTTP优先<br/>spawn回退| C1
    B5 -.->|spawn备用| C2
    A1 -->|HTTP| C3
    B5 --> B7
    B6 --> B7
```

### 1.2 架构设计原则

| 原则 | 说明 |
|------|------|
| 分层解耦 | 前端、后端、AI 引擎三层独立部署，通过标准 HTTP/JSON 接口通信 |
| 技术异构 | 前端 ArkTS + 后端 Node.js + AI 引擎 Python，各取所长 |
| 轻量部署 | YOLOv8n 模型仅 6.25MB，适合工业平板端部署 |
| 容错设计 | 常驻服务优先，spawn 回退，检测超时保护 |
| 数据持久化 | sql.js SQLite 本地存储，无需额外数据库服务 |

---

## 二、前端模块设计

### 2.1 模块结构

```
entry/src/main/ets/
├── common/
│   └── Interfaces.ets          # 类型定义
├── entryability/
│   └── EntryAbility.ets        # 应用入口
└── pages/
    ├── Index.ets               # 主页面（手动模式 + 产线模式双 Tab）
    ├── CameraPage.ets          # 相机拍照页面
    ├── History.ets             # 历史记录 + 统计分析双 Tab
    └── Settings.ets            # 系统设置页面
```

### 2.2 前端类图

```mermaid
classDiagram
    class Index {
        +currentMode: string
        +detectionResult: DetectionResult
        +isDetecting: boolean
        +selectedImage: string
        +sessionStats: SessionStats
        +recentResults: AutoDetectionRecord[]
        +selectImage() void
        +captureCamera() void
        +loadTestImage(index: number) void
        +analyzeImage(imageData: string) void
        +startIndustrialMode() void
        +pauseIndustrialMode() void
        +stopIndustrialMode() void
        +updateSessionStats() void
    }

    class CameraPage {
        +cameraManager: CameraManager
        +previewSurface: Surface
        +initCamera() void
        +startPreview() void
        +takePhoto() string
        +releaseCamera() void
    }

    class History {
        +currentTab: string
        +records: DetectionRecord[]
        +statsData: StatsData
        +filterModel: string
        +filterStatus: string
        +filterStartDate: string
        +filterEndDate: string
        +loadHistory() void
        +deleteRecord(id: string) void
        +deleteAllRecords() void
        +exportCSV() void
        +loadStats() void
    }

    class Settings {
        +serverUrl: string
        +precision: number
        +speed: number
        +captureInterval: number
        +loadConfig() void
        +saveConfig() void
        +testConnection() boolean
    }

    class DetectionResult {
        +success: boolean
        +grade: number
        +gradeMethod: string
        +gradeConfidence: number
        +energyParam: number
        +standbyPower: number
        +defects: DefectInfo
        +position: PositionInfo
        +isPass: boolean
        +labelFound: boolean
        +hasDefect: boolean
        +labelConfidence: number
    }

    class DefectInfo {
        +isDamaged: boolean
        +isStained: boolean
        +isWrinkled: boolean
    }

    class PositionInfo {
        +isCorrect: boolean
        +x: number
        +y: number
        +deviation: number
    }

    class AutoDetectionRecord {
        +id: string
        +timestamp: string
        +result: DetectionResult
        +imageBase64: string
        +duration: number
    }

    class WebcamCameraInfo {
        +name: string
        +url: string
        +isConnected: boolean
    }

    class StatsData {
        +totalDetections: number
        +passRate: number
        +defectDistribution: Map~string, number~
        +gradeDistribution: Map~string, number~
        +avgDuration: number
    }

    Index --> DetectionResult
    Index --> AutoDetectionRecord
    Index --> WebcamCameraInfo
    History --> StatsData
    DetectionResult --> DefectInfo
    DetectionResult --> PositionInfo
```

---

## 三、后端模块设计

### 3.1 目录结构

```
backend/
├── src/
│   ├── server.js                    # Express 入口，端口 3000
│   ├── db/
│   │   └── database.js              # sql.js SQLite 初始化
│   ├── api/
│   │   ├── detectionRoutes.js       # 检测路由
│   │   ├── historyRoutes.js         # 历史路由（GET/DELETE）
│   │   └── productRoutes.js         # 产品路由
│   ├── services/
│   │   ├── detectionService.js      # 检测服务（调用Python）
│   │   └── historyService.js        # 历史服务（CRUD + 统计 + CSV）
│   └── python/
│       ├── detect_server.py         # 常驻HTTP服务，端口5001
│       ├── detect_api.py            # 单次检测脚本（spawn备用）
│       ├── webcam_server.py         # 网络摄像头服务，端口5000
│       └── best.pt                  # YOLOv8n模型权重
└── data/
    └── detection.db                 # SQLite数据库（运行时生成）
```

### 3.2 后端组件图

```mermaid
graph TB
    subgraph Express路由层
        R1[detectionRoutes.js<br/>POST /api/detection/analyze]
        R2[historyRoutes.js<br/>GET/DELETE /api/history]
        R3[productRoutes.js<br/>GET /api/products]
    end

    subgraph 业务服务层
        S1[detectionService.js<br/>图片分析 + Python调用]
        S2[historyService.js<br/>CRUD + 统计 + CSV导出]
        S3[productService.js<br/>产品信息管理]
    end

    subgraph 数据层
        DB[(database.js<br/>sql.js SQLite)]
        T1[products表]
        T2[detection_records表]
    end

    R1 --> S1
    R2 --> S2
    R3 --> S3
    S1 --> DB
    S2 --> DB
    S3 --> DB
    DB --> T1
    DB --> T2
```

### 3.3 检测服务（detectionService.js）

检测服务是系统的核心模块，负责调用 Python AI 引擎执行检测。采用双重调用策略：

| 调用策略 | 描述 | 优势 |
|----------|------|------|
| 常驻服务模式 | HTTP 请求 detect_server.py（端口 5001） | 模型预加载，响应快，无需重复启动 |
| spawn 回退模式 | child_process.spawn 调用 detect_api.py | 无需额外启动服务，兼容性好 |

**调用流程**：

1. 优先尝试 HTTP 请求常驻服务（`http://localhost:5001/detect`）
2. 若常驻服务不可用，回退到 `child_process.spawn` 调用单次脚本
3. 解析返回的 JSON 结果
4. 自动存入历史记录
5. 清理临时文件
6. 60 秒超时保护

### 3.4 历史服务（historyService.js）

提供检测记录的完整生命周期管理：

| 功能 | 方法 | 描述 |
|------|------|------|
| 记录保存 | insertRecord() | 自动保存每次检测结果 |
| 条件查询 | getRecords(filters) | 支持型号、日期范围、状态筛选 |
| 单条删除 | deleteRecord(id) | 按 ID 删除单条记录 |
| 全部删除 | deleteAllRecords() | 清空所有记录 |
| 统计分析 | getStats() | 总数、通过率、缺陷分布、等级分布 |
| CSV 导出 | exportCSV() | 生成标准 CSV 格式文件 |

---

## 四、AI 引擎模块设计

### 4.1 引擎组件

| 组件 | 文件 | 端口 | 描述 |
|------|------|------|------|
| 常驻检测服务 | detect_server.py | 5001 | HTTP 服务，模型预加载，常驻内存 |
| 单次检测脚本 | detect_api.py | - | spawn 模式备用，单次执行后退出 |
| 网络摄像头服务 | webcam_server.py | 5000 | HTTP 服务，网络摄像头画面获取 |
| 模型权重 | best.pt | - | YOLOv8n 模型，6.25MB |

### 4.2 检测流程

```mermaid
flowchart TD
    A[输入图片] --> B[读取图片<br/>cv2.imread]
    B --> C[自动纠偏<br/>EXIF旋转 + 白底分析]
    C --> D[YOLO目标检测<br/>model.predict]
    D --> E{是否检测到标签?}
    E -->|否| F[返回: 未检测到标签]
    E -->|是| G{是否检测到缺陷?<br/>break/stain/wrinkle}
    G -->|是| H[返回: 不合格-含缺陷信息]
    G -->|否| I[裁剪标签区域]
    I --> J[HSV颜色等级检测<br/>5种ROI策略]
    I --> K[PaddleOCR文字识别<br/>4倍放大]
    K --> L[提取能效参数 X.X]
    K --> M[提取待机功率]
    K --> N{OCR识别到X级文字?<br/>置信度>0.8}
    N -->|是| O[采用OCR等级结果]
    N -->|否| P{HSV检测成功?}
    P -->|是| Q[采用HSV等级结果]
    P -->|否| R[等级未知]
    O --> S[位置偏差计算]
    Q --> S
    R --> S
    S --> T[综合判定<br/>等级+缺陷+位置]
    T --> U[返回JSON检测结果]
    H --> U
    F --> U
```

### 4.3 YOLOv8 检测模型

**模型信息**：

| 属性 | 值 |
|------|------|
| 架构 | YOLOv8n（nano） |
| 模型文件 | best.pt |
| 模型大小 | 6.25 MB |
| 输入尺寸 | 640 × 640 |
| 检测类别 | label, nor, break, stain, wrinkle |
| 训练框架 | Ultralytics |
| 推理框架 | PyTorch |

**类别定义**：

| 类别名 | 标识 | 说明 |
|--------|------|------|
| label | 正常标签 | 能效标签正常粘贴 |
| nor | 正常标签（备选） | 训练标注差异产生的备选类 |
| break | 破损 | 标签表面破损/撕裂 |
| stain | 污渍 | 标签表面有污渍/污染 |
| wrinkle | 褶皱 | 标签表面有褶皱/起皱 |

---

## 五、数据库设计

### 5.1 ER 图

```mermaid
erDiagram
    products {
        INTEGER id PK
        TEXT model
        INTEGER energy_grade
        REAL power_consumption
    }

    detection_records {
        INTEGER id PK
        TEXT timestamp
        TEXT product_model
        INTEGER grade
        TEXT energy_param
        TEXT standby_power
        INTEGER is_data_match
        TEXT defects "JSON字段"
        TEXT position "JSON字段"
        INTEGER is_pass
        INTEGER has_defect
        TEXT grade_method
        REAL label_confidence
    }

    products ||--o{ detection_records : "product_model → model"
```

### 5.2 表结构详细设计

#### products 表

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 主键 |
| model | TEXT | NOT NULL | 产品型号 |
| energy_grade | INTEGER | | 能效等级（1-5） |
| power_consumption | REAL | | 功耗参数 |

#### detection_records 表

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 主键 |
| timestamp | TEXT | NOT NULL | 检测时间（ISO 格式） |
| product_model | TEXT | | 产品型号 |
| grade | INTEGER | | 识别的能效等级 |
| energy_param | TEXT | | 能效参数 |
| standby_power | TEXT | | 待机功率 |
| is_data_match | INTEGER | | 参数是否匹配（0/1） |
| defects | TEXT | | 缺陷信息 JSON |
| position | TEXT | | 位置信息 JSON |
| is_pass | INTEGER | | 是否合格（0/1） |
| has_defect | INTEGER | | 是否有缺陷（0/1） |
| grade_method | TEXT | | 等级识别方法（ocr/hsv） |
| label_confidence | REAL | | 标签置信度 |

**defects JSON 字段格式**：

```json
{
  "isDamaged": false,
  "isStained": true,
  "isWrinkled": false
}
```

**position JSON 字段格式**：

```json
{
  "isCorrect": true,
  "x": 320,
  "y": 240,
  "deviation": 3.5
}
```

---

## 六、API 接口设计

### 6.1 API 总览

```mermaid
graph LR
    subgraph 前端
        FE[OpenHarmony App]
    end

    subgraph "后端 API (端口 3000)"
        API1[POST /api/detection/analyze]
        API2[GET /api/history]
        API3[GET /api/history/stats]
        API4[GET /api/history/export/csv]
        API5[DELETE /api/history/:id]
        API6[DELETE /api/history]
    end

    subgraph "AI 引擎"
        AI1[detect_server.py<br/>端口 5001]
        AI2[webcam_server.py<br/>端口 5000]
    end

    FE --> API1
    FE --> API2
    FE --> API3
    FE --> API4
    FE --> API5
    FE --> API6
    FE --> AI2
    API1 --> AI1
```

### 6.2 接口详细设计

#### POST /api/detection/analyze

| 项目 | 说明 |
|------|------|
| 描述 | 接收 Base64 图片并调用 AI 引擎检测 |
| 请求体 | `{ "image": "data:image/jpeg;base64,..." }` |
| 成功响应 | 200 + DetectionResult JSON |
| 失败响应 | 400/500 + 错误信息 |

#### GET /api/history

| 项目 | 说明 |
|------|------|
| 描述 | 获取历史检测记录（支持筛选） |
| 查询参数 | `productModel`（型号）、`startDate`（起始日期）、`endDate`（结束日期）、`status`（合格/不合格） |
| 响应 | 200 + 分页记录列表 |

#### GET /api/history/stats

| 项目 | 说明 |
|------|------|
| 描述 | 获取检测统计数据 |
| 响应 | 200 + 统计对象（totalDetections, passRate, defectDistribution, avgDuration） |

#### GET /api/history/export/csv

| 项目 | 说明 |
|------|------|
| 描述 | 导出检测数据为 CSV 格式 |
| 响应 | 200 + CSV 文件流 |

#### DELETE /api/history/:id

| 项目 | 说明 |
|------|------|
| 描述 | 删除指定 ID 的检测记录 |
| 响应 | 200 + `{ "message": "删除成功" }` |

#### DELETE /api/history

| 项目 | 说明 |
|------|------|
| 描述 | 删除全部检测记录 |
| 响应 | 200 + `{ "message": "全部删除成功" }` |

---

## 七、关键时序图

### 7.1 手动检测时序图

```mermaid
sequenceDiagram
    actor 用户
    participant 前端 as Index.ets
    participant 后端 as server.js
    participant 检测服务 as detectionService.js
    participant AI服务 as detect_server.py<br/>(端口5001)
    participant AI脚本 as detect_api.py<br/>(spawn备用)
    participant 数据库 as SQLite

    用户->>前端: 选择/拍摄图片
    前端->>前端: 图片纠偏(EXIF+白底分析)
    前端->>后端: POST /api/detection/analyze<br/>{image: base64}
    后端->>检测服务: analyzeImage(base64)
    检测服务->>检测服务: 保存临时文件

    alt 常驻服务可用
        检测服务->>AI服务: HTTP POST /detect<br/>{image_path}
        AI服务->>AI服务: YOLO检测→缺陷判定→等级识别→OCR
        AI服务-->>检测服务: JSON检测结果
    else 常驻服务不可用
        检测服务->>AI脚本: child_process.spawn<br/>python detect_api.py
        AI脚本->>AI脚本: YOLO检测→缺陷判定→等级识别→OCR
        AI脚本-->>检测服务: stdout JSON结果
    end

    检测服务->>数据库: INSERT detection_records
    检测服务->>检测服务: 清理临时文件
    检测服务-->>后端: DetectionResult
    后端-->>前端: 200 + JSON
    前端->>前端: 展示检测报告
    前端-->>用户: 显示结果(等级/缺陷/位置/OCR)
```

### 7.2 产线自动检测时序图

```mermaid
sequenceDiagram
    actor 主管
    participant 前端 as Index.ets
    participant 摄像头服务 as webcam_server.py<br/>(端口5000)
    participant 后端 as server.js
    participant 检测服务 as detectionService.js
    participant AI服务 as detect_server.py<br/>(端口5001)
    participant 数据库 as SQLite

    主管->>前端: 进入产线模式
    前端->>摄像头服务: GET /video_feed
    摄像头服务-->>前端: MJPEG 视频流预览
    主管->>前端: 配置间隔(5/10/30/60s) → 开始检测

    loop 定时拍照循环
        前端->>摄像头服务: GET /capture
        摄像头服务-->>前端: 拍摄图片(base64)
        前端->>后端: POST /api/detection/analyze
        后端->>检测服务: analyzeImage(base64)
        检测服务->>AI服务: HTTP POST /detect
        AI服务-->>检测服务: JSON检测结果
        检测服务->>数据库: INSERT detection_records
        检测服务-->>后端: DetectionResult
        后端-->>前端: 200 + JSON
        前端->>前端: 更新统计面板<br/>(总数/通过率/缺陷分布/耗时)
    end

    主管->>前端: 暂停/结束检测
    前端->>前端: 停止定时器，显示会话统计
```

### 7.3 历史记录查询时序图

```mermaid
sequenceDiagram
    actor 工程师
    participant 前端 as History.ets
    participant 后端 as server.js
    participant 历史服务 as historyService.js
    participant 数据库 as SQLite

    工程师->>前端: 进入历史记录页面
    前端->>后端: GET /api/history?productModel=&startDate=&endDate=&status=
    后端->>历史服务: getRecords(filters)
    历史服务->>数据库: SELECT with filters
    数据库-->>历史服务: 记录列表
    历史服务-->>后端: 格式化记录
    后端-->>前端: 200 + JSON分页数据
    前端-->>工程师: 展示记录列表

    工程师->>前端: 切换到统计Tab
    前端->>后端: GET /api/history/stats
    后端->>历史服务: getStats()
    历史服务->>数据库: SELECT COUNT/GROUP BY
    数据库-->>历史服务: 统计数据
    历史服务-->>后端: StatsData
    后端-->>前端: 200 + JSON
    前端-->>工程师: 展示统计图表

    opt 导出CSV
        工程师->>前端: 点击导出CSV
        前端->>后端: GET /api/history/export/csv
        后端->>历史服务: exportCSV()
        历史服务->>数据库: SELECT *
        数据库-->>历史服务: 全部记录
        历史服务-->>后端: CSV文件流
        后端-->>前端: 200 + CSV
        前端-->>工程师: 保存CSV文件
    end
```

---

## 八、安全设计

### 8.1 传输安全

- 生产环境启用 HTTPS
- API 接口限流防刷
- 文件上传限制 50MB

### 8.2 数据安全

- 临时图片检测后立即删除
- SQLite 数据库文件本地存储，不暴露到网络
- Base64 图片数据不在日志中记录

### 8.3 系统安全

- 内网部署，不暴露到公网
- Python 子进程超时保护（60 秒）
- 常驻服务异常时自动回退 spawn 模式

---

## 九、性能设计

### 9.1 前端性能

- 列表分页加载，避免大数据量渲染
- 图片压缩后再上传
- 检测结果本地缓存

### 9.2 后端性能

- Express 中间件 JSON 解析限制 50MB
- 检测结果内存缓存
- 常驻服务模式避免模型重复加载

### 9.3 AI 引擎性能

- YOLOv8n 轻量模型，推理时间 < 2 秒
- detect_server.py 模型预加载，常驻内存
- OCR 前 4 倍放大，平衡精度与性能
- HSV 分析纯 NumPy 运算，毫秒级完成

---

## 十、部署设计

### 10.1 硬件要求

| 组件 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU | 4 核 | 8 核 |
| 内存 | 4 GB | 8 GB |
| 存储 | 2 GB | 10 GB |
| 相机 | 500 万像素 | 800 万像素以上 |

### 10.2 软件要求

| 组件 | 版本 |
|------|------|
| OpenHarmony | API 20 |
| Node.js | v18+ |
| Python | 3.8+ |
| YOLOv8 | ultralytics >= 8.0 |
| PaddleOCR | >= 2.6 |
| OpenCV | >= 4.5 |

### 10.3 服务端口规划

| 服务 | 端口 | 说明 |
|------|------|------|
| Express 后端 | 3000 | 主服务，API 路由入口 |
| AI 常驻检测服务 | 5001 | detect_server.py，模型预加载 |
| 网络摄像头服务 | 5000 | webcam_server.py，摄像头画面获取 |

### 10.4 网络配置

| 环境 | 后端地址 |
|------|----------|
| 开发（模拟器） | http://10.0.2.2:3000 |
| 开发（真机） | http://{PC_IP}:3000 |
| 生产 | http://{SERVER_IP}:3000 |
